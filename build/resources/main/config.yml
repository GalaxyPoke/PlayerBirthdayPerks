# ================================
# PlayerBirthdayPerks 配置文件
# 仿王者荣耀生日福利系统
# ================================

# 数据库配置
database:
  # 数据库类型: sqlite 或 mysql
  type: sqlite
  
  # SQLite配置 (当type为sqlite时使用)
  sqlite:
    file: data.db
  
  # MySQL配置 (当type为mysql时使用)
  mysql:
    host: localhost
    port: 3306
    database: birthday_perks
    username: root
    password: password
    # 连接池配置
    pool:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

# 生日设置
birthday:
  # 是否允许修改生日 (false则只能设置一次)
  allow-modify: false
  # 每年允许修改次数 (-1为无限制, 仅当allow-modify为true时有效)
  modify-limit-per-year: 1
  # 生日福利领取时间窗口 (天数, 过期后无法领取当年福利)
  claim-window-days: 7

# 福利配置
rewards:
  # 是否启用福利系统
  enabled: true
  
  # 生日当天登录提醒
  login-notification: true
  
  # 全服广播生日祝福
  broadcast:
    enabled: true
    message: "&6&l🎂 今天是 &e%player% &6&l的生日！让我们一起祝TA生日快乐！🎉"
  
  # 烟花效果
  firework:
    enabled: true
    amount: 3
    delay-ticks: 20
  
  # 音效
  sound:
    enabled: true
    type: UI_TOAST_CHALLENGE_COMPLETE
    volume: 1.0
    pitch: 1.0
  
  # 物品奖励
  items:
    - material: CAKE
      amount: 1
      name: "&d&l🎂 生日蛋糕"
      lore:
        - "&7专属于你的生日蛋糕"
        - "&7祝你生日快乐！"
        - ""
        - "&8获得时间: %date%"
    - material: DIAMOND
      amount: 5
      name: "&b&l💎 生日钻石"
      lore:
        - "&7生日专属奖励"
    - material: EXPERIENCE_BOTTLE
      amount: 16
      name: "&a&l✨ 生日经验瓶"
      lore:
        - "&7生日快乐！"
  
  # 执行命令奖励 (以控制台身份执行)
  commands:
    - "give %player% minecraft:firework_rocket{Fireworks:{Flight:2}} 8"
    # - "eco give %player% 1000"
    # - "lp user %player% permission settemp birthday.vip true 1d"
  
  # 经验奖励
  experience:
    enabled: true
    amount: 500
  
  # 金钱奖励 (需要Vault支持)
  money:
    enabled: false
    amount: 1000.0

# 头像框系统 (模拟)
avatar-frame:
  enabled: true
  # 头像框持续时间 (天数, -1为永久)
  duration-days: 30
  # 头像框名称 (用于聊天前缀等)
  prefix: "&6🎂&r "

# 缓存配置
cache:
  # 玩家数据缓存过期时间 (分钟)
  expire-minutes: 30
  # 最大缓存数量
  max-size: 1000

# 调试模式
debug: false
